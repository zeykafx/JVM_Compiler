$Good luck

final message string = "Hello";
final run bool = true;

final a int = 3;
final b float = a * 2.0;
final b bool = a == 3;

Point rec {
    x int;
    y int;
}

$a int = 3;
$c float = 5;

fun square(v int) int {
    return v*v;
}

fun main() {
    value int = readInt();
    p Point = Point(a, a+value);
    writeInt(square(value));
    writeln("");
    i int;
    for (i, 1, a, 1) {
        while (value!=0) {
            if (run){
                value = value - 1;
            } else {
                write(message);
            }
        }
    }
    i = (i+2)*2;
}